我需要调整 index.html 中各 <section> 的显示顺序，以符合用户浏览和下单的心理逻辑。

请按照以下严格的顺序重新排列 <body> 标签内的所有 Section 代码块（不要修改 Section 内部代码，只调整它们在 HTML 文件中的上下位置）：

Navbar (<nav>) - 保持顶部

Hero 区域 (<section class="relative pt-32...">) - 首屏核心

会员权益 (<section id="features">) - 先展示产品价值（Bento Grid），让用户想要。

全球比价 (<section id="global-price">) - 再展示价格对比，突出性价比。

核心价值 (<section id="why-shared">) - 解释为什么合租是聪明的选择。

价格方案 (<section id="pricing">) - 用户心动了，直接放购买链接（转化核心）。

避坑指南 (<section id="safety-guide">) - 放在价格后，作为信任背书，消除“怕被骗”的顾虑。

注意事项 (<section id="notices">) - 防封号提示。

故障排查 (<section id="troubleshooting">) - 售后技术支持。

常见疑问 (<section id="faq">) - 最后的兜底问答。

Footer (<footer>) - 保持底部

总结操作： 请把原本在上面的 safety-guide 移到 pricing 下面；把 features 提到 hero 之后。确保逻辑流是：“产品种草 -> 价格诱惑 -> 立即购买 -> 安全信任 -> 售后支持”。