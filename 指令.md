“我现在决定优化网站性能，把 index.html 里的 Tailwind CDN 模式切换为本地静态 CSS 模式。

现在的项目结构是：根目录下有 index.html，还有 blog/ 文件夹。

请一步步指导我完成以下操作：

环境安装：请给我 npm 命令来安装 tailwindcss（假设我已经有 Node 环境，如果没有请告诉我怎么下载）。

配置文件：帮我生成 tailwind.config.js，重点是 content 数组要写对，确保能扫描到 ./*.html 和 ./blog/*.html 以及 ./*.py (因为我的 python 脚本里也有 html 模板)。

CSS 入口：帮我创建 src/input.css 代码。

生成命令：请给我一个完整的终端命令，用于把 CSS 编译并输出到 assets/style.css，同时开启 --watch 监听模式，方便我后续修改。

替换引用：告诉我如何修改 index.html 和 build.py 里的模板，把 <script src="https://cdn.tailwindcss.com"></script> 删掉，换成标准的 <link> 标签引用新生成的 CSS。

请开始指导。”